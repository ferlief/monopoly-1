import javax.swing.*;

import java.awt.*;

public class houseHolder extends JPanel
{
	//ImageIcon houseIcon = new ImageIcon(getClass().getResource("house.png"));
	//JLabel houseLabel = new JLabel(houseIcon);
	//ImageIcon hotelIcon = new ImageIcon(getClass().getResource("hotel.png"));
	//JLabel hotelLabel = new JLabel(hotelIcon);	
	
	houseHolder()
	{
		setPreferredSize(new Dimension(100,25));
		setBackground(Color.white);
		setLayout(new FlowLayout(FlowLayout.LEFT,0,0));
	}
	
	public void addHouse()
	{
		
	}
	
	public void addHotel()
	{
		
	}
}
